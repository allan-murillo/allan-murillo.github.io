# Database Starter

This folder is prepared for your database layer.

## Recommended Setup

- Database: `PostgreSQL`
- ORM: `Prisma`

## Next Step

After installing dependencies, generate your first migration from the Prisma schema in `database/prisma/schema.prisma`.
